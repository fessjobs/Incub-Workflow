# PostgreSQL-Verbindung
DATABASE_URL="postgresql://incubworkflow:incubworkflow@localhost:5432/incubworkflow"

# Geheimnis zum Signieren der Session-Cookies (min. 32 Zeichen, z. B. via `openssl rand -hex 32`)
AUTH_SECRET="bitte-aendern-mindestens-32-zeichen-lang"

# Zugangsdaten des initialen Admin-Kontos (wird beim Seed angelegt)
ADMIN_EMAIL="admin@incub.live"
ADMIN_PASSWORD="incub2026!"
ADMIN_NAME="Maik Janke"

# Wurzelverzeichnis der Beleg-Ablage (Ordnerstruktur laut Spec, Abschnitt 7)
STORAGE_PATH="./incubWorkflow-Ablage"

# Claude API für das automatische Auslesen der Belege (Sprint 2).
# Ohne Schlüssel funktioniert alles, die Felder werden dann manuell erfasst.
ANTHROPIC_API_KEY=""
# Optional: Modell für die Extraktion (Standard: claude-opus-4-8)
ANTHROPIC_MODEL="claude-opus-4-8"

# ── Modul Einsätze & Stundennachweise ─────────────────────────────────────
# Öffentliche Basis-URL für Mitarbeiter-Links (Railway-Domain), z. B. https://app.example.com
APP_BASE_URL=""
# Modell für den Konkretisierungs-Parser (Standard: claude-sonnet-4-6)
ANTHROPIC_PARSER_MODEL="claude-sonnet-4-6"
# E-Mail-Versand der Mitarbeiter-Links (optional; ohne SMTP nur WhatsApp-Text zum Kopieren)
SMTP_URL=""
MAIL_FROM="fess.jobs Dispo <dispo@fess.jobs>"
# Bearer-Token für externe Cron-Aufrufe von POST /api/jobs/run (optional; der Worker
# läuft auch im Server-Prozess, JOBS_WORKER=off schaltet ihn ab)
JOBS_SECRET=""
# Optionaler S3-kompatibler Speicher für Unterschriften (Standard: Datenbank)
S3_BUCKET=""
S3_ENDPOINT=""
S3_REGION="auto"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
